node_modules/
coverage/
assets/
dist/
venv/
js/vendor/
js/viewer.min.js
.husky/_/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.idea/
.vscode/
.git/
pnpm-lock.yaml

#Owned/auto-recorded by the Jules agent — read-only for us (see CLAUDE.md)
.jules/

#Gitignored local Claude config (see .gitignore). CI's staged-files prettier
#hook never sees it, so don't let whole-repo `make fmt-check` fail on it.
#Scoped to local files only — tracked .claude/commands/ stays format-checked.
.claude/settings.local.json
