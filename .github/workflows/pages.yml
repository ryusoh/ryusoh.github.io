name: Deploy to GitHub Pages

on:
    push:
        branches: ['master']
    workflow_dispatch:

permissions:
    contents: read
    pages: write
    id-token: write

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    deploy:
        runs-on: ubuntu-latest
        env:
            FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url || steps.deployment-retry.outputs.page_url }}
        steps:
            - name: Checkout
              uses: actions/checkout@v7

            - name: Stamp service worker cache version
              run: |
                  stamp=$(git rev-parse --short HEAD)
                  sed -i "s/const CACHE_NAME = .*/const CACHE_NAME = 'ryusoh-cache-${stamp}';/" sw.js

            - name: Setup Pages
              uses: actions/configure-pages@v6

            - name: Upload artifact
              uses: actions/upload-pages-artifact@v5
              with:
                  path: '.'

            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v5
              continue-on-error: true

            # The Pages backend intermittently rejects deployments with
            # "Deployment failed, try again later." — a fresh attempt clears it.
            - name: Retry deploy once
              id: deployment-retry
              if: steps.deployment.outcome == 'failure'
              uses: actions/deploy-pages@v5
